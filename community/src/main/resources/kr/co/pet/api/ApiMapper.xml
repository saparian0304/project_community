<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.co.pet.api.ApiMapper">
	
	<insert id="apiInsert" parameterType="kr.co.pet.api.ApiVO">
		INSERT INTO board (
			board_name, title, content, horse_hair, board_no
		) VALUES (
			#{board_name}, #{title}, #{content}, #{horse_hair}, (SELECT board_no FROM board WHERE board_no = #{board_no})
		) 
		
	</insert>
	
</mapper>